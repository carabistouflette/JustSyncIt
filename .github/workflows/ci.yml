name: CI/CD Pipeline

on:
  push:
    branches: [ '**' ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ '**' ]

permissions:
  checks: write
  contents: read
  pull-requests: write
  packages: write
  security-events: write

env:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false -Dorg.gradle.workers.max=2"
  GRADLE_USER_HOME: ${{ github.workspace }}/.gradle

jobs:
  build-and-test:
    name: Build & Test (Java ${{ matrix.java-version }})
    runs-on: ${{ matrix.os }}
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        java-version: [21, 24]
        run-quality-checks: [false]
        include:
          # Only run code quality checks on Ubuntu with Java 21
          - os: ubuntu-latest
            java-version: 21
            run-quality-checks: true
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Needed for semantic release
        
    - name: Set up JDK ${{ matrix.java-version }}
      uses: actions/setup-java@v4
      with:
        java-version: ${{ matrix.java-version }}
        distribution: 'temurin'
        
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
          .gradle
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Grant execute permission for gradlew
      if: runner.os == 'Linux'
      run: chmod +x gradlew
      
    - name: Run dev build
      run: ./gradlew devBuild --no-daemon -PjavaVersion=${{ matrix.java-version }}
       
    - name: Run unit tests with coverage
      run: ./gradlew clean test jacocoTestReport --no-daemon -PjavaVersion=${{ matrix.java-version }}
       
    - name: Run integration tests
      run: ./gradlew integrationTest --no-daemon -PjavaVersion=${{ matrix.java-version }}
       
    - name: Verify performance tests are excluded
      run: |
        echo "Verifying that performance tests and benchmarks are properly excluded from CI..."
        # Check that performance test tasks exist but are not part of the check task
        ./gradlew tasks --group=verification | grep -E "(performanceTest|benchmarkTest)" && echo "Performance test tasks found" || echo "Performance test tasks not found"
        # Verify that check task doesn't include performance tests
        ./gradlew check --dry-run 2>&1 | grep -E "(performanceTest|benchmarkTest)" && echo "ERROR: Performance tests found in check task" || echo "OK: Performance tests excluded from check task"
       
    - name: Check test results
      run: |
        if [ "$RUNNER_OS" == "Linux" ] || [ "$RUNNER_OS" == "macOS" ]; then
          ls -la build/test-results/test/ || echo "Test results directory not found"
        else
          dir build\test-results\test\ || echo "Test results directory not found"
        fi
      shell: bash
       
    - name: Run Checkstyle
      if: matrix.run-quality-checks
      run: ./gradlew checkstyleMain checkstyleTest --no-daemon -PjavaVersion=${{ matrix.java-version }}
       
    - name: Run SpotBugs
      if: matrix.run-quality-checks
      run: ./gradlew spotbugsMain spotbugsTest --no-daemon -PjavaVersion=${{ matrix.java-version }}
      
    - name: Generate unit test report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Unit Tests (${{ matrix.os }}, Java ${{ matrix.java-version }})
        path: build/test-results/test/TEST-*.xml
        reporter: java-junit
        
    - name: Generate integration test report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Integration Tests (${{ matrix.os }}, Java ${{ matrix.java-version }})
        path: build/test-results/integrationTest/TEST-*.xml
        reporter: java-junit
        
    - name: Upload coverage reports to Codecov
      if: matrix.java-version == '21' && matrix.os == 'ubuntu-latest'
      uses: codecov/codecov-action@v4
      with:
        file: ./build/reports/jacoco/test/jacocoTestReport.xml
        flags: unittests
        name: codecov-umbrella
        
    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results-${{ matrix.os }}-java${{ matrix.java-version }}-${{ github.run_attempt }}
        path: |
          build/reports/tests/test/
          build/reports/tests/integrationTest/
          build/reports/checkstyle/
          build/reports/spotbugs/
          build/reports/jacoco/
        overwrite: true
          
    - name: Run release build (only on main branch, Java 21)
      if: matrix.java-version == '21' && matrix.os == 'ubuntu-latest' && github.ref == 'refs/heads/main'
      run: ./gradlew releaseBuild --no-daemon -PjavaVersion=${{ matrix.java-version }}
      
    - name: Build packages (only on main branch, Java 21)
      if: matrix.java-version == '21' && matrix.os == 'ubuntu-latest' && github.ref == 'refs/heads/main'
      run: |
        # Install package building dependencies
        sudo apt-get update
        sudo apt-get install -y dpkg-dev rpm
        
        # Build packages
        ./gradlew buildPackages --no-daemon -PjavaVersion=${{ matrix.java-version }}
      
    - name: Upload JAR artifact (only on main branch, Java 21)
      if: matrix.java-version == '21' && matrix.os == 'ubuntu-latest' && github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@v4
      with:
        name: justsyncit-jar-java21-${{ github.run_attempt }}
        path: build/libs/*.jar
        overwrite: true
        
    - name: Upload package artifacts (only on main branch, Java 21)
      if: matrix.java-version == '21' && matrix.os == 'ubuntu-latest' && github.ref == 'refs/heads/main'
      uses: actions/upload-artifact@v4
      with:
        name: justsyncit-packages-java21-${{ github.run_attempt }}
        path: |
          *.deb
          *.rpm
          *.AppImage
        overwrite: true

  # Dependency check is disabled in build.gradle (skip = true)
  # Keeping this job commented out for future use if needed
  # dependency-check:
  #   name: Dependency Vulnerability Scan
  #   runs-on: ubuntu-latest
  #   needs: build-and-test
  #   if: false # Disabled until NVD API issues are resolved
  #
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #
  #   - name: Set up JDK 21
  #     uses: actions/setup-java@v4
  #     with:
  #       java-version: 21
  #       distribution: 'temurin'
  #
  #   - name: Cache Gradle packages
  #     uses: actions/cache@v4
  #     with:
  #       path: |
  #         ~/.gradle/caches
  #         ~/.gradle/wrapper
  #       key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
  #       restore-keys: |
  #         ${{ runner.os }}-gradle-
  #
  #   - name: Run OWASP Dependency Check
  #     run: ./gradlew dependencyCheckAggregate --no-daemon -PjavaVersion=21
  #
  #   - name: Upload dependency check results
  #     uses: actions/upload-artifact@v4
  #     if: always()
  #     with:
  #       name: dependency-check-results
  #       path: build/reports/dependency-check/

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'security-scan')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
        
    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Log in to Docker Hub
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: justsyncit/app
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-and-test, docker-build]
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: 21
        distribution: 'temurin'
        
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Install package building dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y dpkg-dev rpm
        
    - name: Build release artifacts
      run: |
        ./gradlew releaseBuild --no-daemon -PjavaVersion=21
        ./gradlew buildPackages --no-daemon -PjavaVersion=21
      
    - name: List artifacts
      run: |
        echo "Built artifacts:"
        find build -name "*.jar" -o -name "*.tar.gz" -o -name "*.zip" -o -name "*.md5" -o -name "*.sha256" | sort
        echo "Package artifacts:"
        ls -la *.deb *.rpm *.AppImage 2>/dev/null || echo "No packages found"
      
    - name: Generate changelog
      id: changelog
      run: |
        # Extract version from tag
        VERSION=${GITHUB_REF#refs/tags/v}
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        
        # Generate changelog (simplified version)
        echo "## Changes in v$VERSION" > CHANGELOG.md
        echo "" >> CHANGELOG.md
        git log --pretty=format:"- %s" $(git describe --tags --abbrev=0 HEAD^)..HEAD >> CHANGELOG.md
        
    - name: Create GitHub Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ steps.changelog.outputs.version }}
        body_path: CHANGELOG.md
        draft: false
        prerelease: false
        
    - name: Upload JAR asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: build/libs/justsyncit-${{ steps.changelog.outputs.version }}.jar
        asset_name: justsyncit-${{ steps.changelog.outputs.version }}.jar
        asset_content_type: application/java-archive
        
    - name: Upload Fat JAR asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: build/libs/justsyncit-${{ steps.changelog.outputs.version }}-all.jar
        asset_name: justsyncit-${{ steps.changelog.outputs.version }}-all.jar
        asset_content_type: application/java-archive
        
    - name: Upload TAR.GZ asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: build/distributions/justsyncit-${{ steps.changelog.outputs.version }}.tar.gz
        asset_name: justsyncit-${{ steps.changelog.outputs.version }}.tar.gz
        asset_content_type: application/gzip
        
    - name: Upload ZIP asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: build/distributions/justsyncit-${{ steps.changelog.outputs.version }}.zip
        asset_name: justsyncit-${{ steps.changelog.outputs.version }}.zip
        asset_content_type: application/zip
        
    - name: Upload DEB asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: justsyncit_${{ steps.changelog.outputs.version }}_all.deb
        asset_name: justsyncit_${{ steps.changelog.outputs.version }}_all.deb
        asset_content_type: application/vnd.debian.binary-package
        
    - name: Upload RPM asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: justsyncit-${{ steps.changelog.outputs.version }}-1.noarch.rpm
        asset_name: justsyncit-${{ steps.changelog.outputs.version }}-1.noarch.rpm
        asset_content_type: application/x-rpm
        
    - name: Upload AppImage asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: JustSyncIt-${{ steps.changelog.outputs.version }}-x86_64.AppImage
        asset_name: JustSyncIt-${{ steps.changelog.outputs.version }}-x86_64.AppImage
        asset_content_type: application/x-executable
        
    - name: Upload checksums
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: build/libs/justsyncit-${{ steps.changelog.outputs.version }}-all.jar.sha256
        asset_name: justsyncit-${{ steps.changelog.outputs.version }}-all.jar.sha256
        asset_content_type: text/plain